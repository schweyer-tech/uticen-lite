# Learnings Index — uticen-lite

Durable, hard-won engineering rules harvested from this repo. Each row links to a file with full
context, the enforceable rule, and a reference. Read the relevant file by ID before doing work in
that area; treat active rules as binding. Newest entries are on top.

> ⚑ **[0001](0001-stay-compatible-with-the-uticen-app.md) is the cardinal rule** — read it before
> any change that touches the bundle. The SDK exists to feed the Uticen app; staying
> bundle-compatible (`contract/bundle.schema.json`) is the one compatibility surface that must never
> silently drift.

| ID | Date | Area | Rule (one line) | Status |
| --- | --- | --- | --- | --- |
| [0046](0046-feed-coverage-to-the-sonar-scan-and-fetch-a-relocatable-python-on-self-hosted-macos.md) | 2026-06-29 | ci | A **0% coverage tile on SonarQube means no report is uploaded, NOT untested code** — `sonar-scanner` never runs tests; generate it in the scan workflow (`pytest --cov-report=xml`), declare `pytest-cov` in the `dev` extra, and point `sonar.python.coverage.reportPaths=coverage.xml` at it (path relative to project base). Build the coverage venv from an **editable** `pip install -e ".[adapters,dev]"` so the package-metadata-dependent CLI version test resolves (`importlib.metadata.version`). **`actions/setup-python` cannot provision Python on a self-hosted macOS runner whose user is not `runner`** — its macOS build is not relocatable (hardcodes `/Users/runner/hostedtoolcache`, dies `mkdir: /Users/runner: Permission denied`) and no `RUNNER_TOOL_CACHE`/`AGENT_TOOLSDIRECTORY` redirect fixes it; instead probe for a local `python3.1x`, else download a **pinned relocatable** `python-build-standalone` `install_only` tarball (arch from `uname -m`) into `$RUNNER_TEMP` and build the venv from `python/bin/python3`. Drove dashboard coverage 0% → 92.4%. | active |
| [0045](0045-fix-a-dtype-comparison-failure-in-the-data-not-by-coercing-one-equivalence-bound-path.md) | 2026-06-29 | testing | Fix a dtype/comparison failure in the **DATA** (the source column's declared `data_type`), **never** by coercing in only ONE of two equivalence-bound paths: `_mask_expr`-generated `test()` and the `_condition_mask` interpreter must stay equal ([[0009]]), so wrapping gt/ge/lt/le with `pd.to_numeric` in the generated path alone silently diverges them — and the equivalence fixtures may not cover the text-dtype-numeric case, so the suite stays green while the contract breaks (ordering ops on a text column are *meant* to raise — [[0011]] — deliberate author feedback, not a thing to coerce away). Root cause here was an **unfaithful test seed**: seed a control-plane source through the real `POST /sources/{id}` save route, NOT `repo.set_columns` — the save route writes the per-column flags AND `key_config` (the loader derives `Population.key_columns` from `key_config`, so set_columns alone leaves the key unresolved) AND `data_type` (set_columns leaves columns text-typed, so numeric comparisons fail downstream). A render-only feature that must touch `render_rule.py` / the bundle schema / the store schema to make a test pass is a signal the **test** is wrong — re-seed, don't patch production. | active |
| [0044](0044-must-be-identical-sibling-chrome-lives-in-one-shared-partial-not-parallel-copies.md) | 2026-06-29 | frontend | Chrome that must render **identically across sibling pages/tabs** (the control editor's Definition/Logic/History header — editable title + pencil + Run + tab strip) lives in **ONE included partial**, never N hand-rolled copies: parallel copies silently drift when one is redesigned (Logic/History fell behind Definition's editable-title+Run card). Move **both the markup AND its chrome-specific inline `<script>`** into the partial and **delete the script from every origin** (else one page double-binds the handler / duplicates an element `id`); pass caller-specific bits (active-tab key, per-page fallback) as context. The cure for "thread the change through every copy" is to have one copy — audit-every-site family ([[0014]]/[[0038]]); restyle a shared *structural class* still via a modifier class ([[0005]]). | active |
| [0043](0043-drive-a-multi-element-per-instance-accent-from-one-css-custom-property-with-token-fallback.md) | 2026-06-29 | frontend | Drive a **dynamic per-instance accent** that several sub-elements share (the procedure card's left **stripe** + the **code-chip** tint) from ONE CSS **custom property** on the container (`style="--proc:{{ color }}"`), each sub-element reading `var(--proc, <static-token>)` with a static fallback — never thread the colour inline onto each sub-element (the audit-every-site trap [[0014]]); one property cascades to all descendants. Always give the fallback so an instance lacking the property renders neutral — in particular a **client-JS-built** section (`newProcedureSection()`) that can't know the server-assigned palette colour degrades neutral until the next server render (document it so nobody invents a client-side colour). Derive muted variants from the **same** property via `color-mix(in srgb, var(--proc,<bg>) N%, <bg>)` (one source of truth; fine for a localhost target). Routes static chrome through `var(--token)` as before. [[0005]]/[[0032]] | active |
| [0042](0042-never-refresh-an-element-from-the-htmx-load-event-it-fires-on-its-own-swap.md) | 2026-06-29 | frontend | Never re-fetch/refresh an element from inside an `htmx:load` (or `afterSwap`) handler that swaps **that same element** — `htmx:load` fires on every swap, including the element's own, so it re-triggers itself into an **infinite request loop**. The header update-indicator did exactly this (`htmx:load` → `htmx.ajax` outerHTML swap → `htmx:load` → …): ~4000 `/updates/indicator` requests in seconds, button perpetually re-rendered so the modal never opened. Masked by the toggle defaulting OFF (empty response self-terminates) and by a prior cross-thread 500 ([[0002]]) breaking the swap. A base-layout element (header/nav) is never part of a partial swap → load it **once on page load + a `setInterval` poll**, drop the per-swap refresh; if swap-driven refresh is needed, guard `evt.target.closest()` or use plain `fetch`. Only driving the real page (request count / clickability) catches it — `TestClient` can't. [[0040]] **Corollary (2026-06-29):** an `outerHTML` swap of an **empty** body **deletes its own target** — the indicator placeholder vanished until reload whenever `/updates/indicator` returned empty (the toggle OFF); return a stable non-empty wrapper (or swap `innerHTML`) if a base-layout slot must survive an empty state. PR #110 sidesteps the common case by defaulting the check ON ([[0017]]). | active |
| [0041](0041-control-verdict-must-derive-from-the-shared-threshold-determination-not-a-raw-failed-count.md) | 2026-06-28 | render | Every pass/fail **verdict** (and its status colours) on ANY control-plane surface — run view, dashboard badge, history, email — must derive from the shared threshold-based `Determination` the workpaper uses, **never** from a raw `failed == 0`. The run view branched on `run.failed` and the dashboard badge on `'pass' if failed == 0` while never loading the control's `Threshold`, so a run within tolerance (4/30 = 13.33% ≤ 15%) showed a red "Operated with deficiencies" pill **directly above** the embedded workpaper's green "Operated effectively" — two opposite verdicts for one run. Load the `Threshold` in the route, pass a computed `passed`/`verdict` to the template, colour tiles/badges off the same `passed`, surface the threshold text, and pin it with a non-zero-threshold within-tolerance test. Render-parity kin of [[0036]]. | active |
| [0040](0040-js-string-literals-in-templates-crash-the-whole-inline-script-verify-with-pageerror.md) | 2026-06-28 | frontend | A bad backslash-escape in a **JS string literal emitted from a server template** (e.g. a tooltip `title` in `newProcedureSection()`'s concatenated `innerHTML`) crashes the **ENTIRE inline `<script>`** — `\\'` is escaped-backslash + string-close, not an escaped quote — silently killing `bindCards()` and EVERY delegated handler (insert/proc-add/proc-del/pencil/collapse). It's **invisible to diff review** (which mis-reads `\\'` as `\'`) and to the markdown plan it was transcribed from; only the **browser e2e** (`page.on('pageerror')`) caught it. Verify JS string emission with a parser (`node --check`) or a zero-pageerror e2e assert; better, **keep human prose OUT of hand-concatenated JS** — put tooltip/label text in a `data-*`/HTML attribute the JS reads, or build DOM via `createElement`/`textContent`. Run-the-real-thing kin of [[0009]]/[[0012]]. **Corollary (2026-06-29):** the template + JS-builder twins are coupled by any whole-PAGE assertion — `assert "<retired-literal>" not in page` fails unless BOTH change (the inline `<script>` source is part of the page text), so keep one as a cross-site drift guard AND scope both render sites into the **same task** (a split mis-sizes it — the whole-page test drags the JS builder into Task 1); factor shared prose once (`{% set %}` + same JS literal). | active |
| [0039](0039-a-plan-that-deviates-from-its-spec-must-enumerate-every-dropped-spec-requirement.md) | 2026-06-28 | process | A plan that deviates from its spec/design (a correction, simplification, or scope cut) MUST explicitly enumerate every **dropped spec-mandated requirement** — separating *removed work* (steps no longer needed) from *dropped requirements* (assertions/tests/behaviors the spec demanded); a spec-mandated **assertion** is a requirement, never "work" to drop silently. A "Corrections" note that lists only removed work hides a dropped assertion (here: the spec's `assert population == 5` for the intentional 17→5 distinct-items-examined shift was omitted from every task and only the final review caught it). Re-read the spec's Testing-strategy / "own it explicitly" sections and map each named assertion to a task before finishing the plan ([[0031]], [[0012]]). | active |
| [0038](0038-thread-a-new-context-key-through-every-render-site-of-a-shared-partial.md) | 2026-06-28 | frontend | When a template partial is rendered from more than one endpoint, thread every new context key through **ALL** of its render sites — the full-page GET **and** every HTMX swap handler that re-renders it — or the feature renders on load and then silently vanishes after the first swap from a site that forgot the key (invisible to diff review + load-time checks). `grep` every `TemplateResponse(..., "<partial>.html", ...)` + `{% include %}` site; better, **centralize the partial's context in one builder** (`_card_bands` beside `_procedure_context`) every site calls so they can't drift. Render-context member of the audit-every-site family ([[0014]], [[0023]]); caught by the post-swap e2e ([[0012]]). | active |
| [0037](0037-assert-client-persisted-write-path-never-fake-state-to-dodge-a-race.md) | 2026-06-28 | testing | Test the **WRITE** path of app-persisted client state by **waiting on the app's own write** (`page.wait_for_function("k => localStorage.getItem(k) === <expected>", arg=key)`) — which removes the event/render-race flake AND asserts the write; **never inject the state with a manual `setItem`/`setState`** to dodge the race (it silently deletes write-path coverage, so a broken writer still goes green). Assert the write *before* reload, keep the post-reload restore assertion — both halves must be covered; a `setItem` in an e2e body is a smell. Strengthens the load-bearing e2e ([[0012]]). | active |
| [0036](0036-sole-auto-derived-positional-label-is-empty-for-render-bundle-parity.md) | 2026-06-28 | contract | Auto-derive a positional label (P1..Pn) **only when ≥2 items exist**; a **sole** auto-derived item carries an **empty** label so the single-item render stays byte-identical to the pre-feature form AND matches the bundle's single-item default (which emits an empty label) — a lone "P1" silently changes existing output and diverges the local render from the exported bundle. Author-**defined** labels always keep their own value; pin it with a test asserting the sole label is empty + the legacy (no-prefix) render form ([[0001]], [[0015]]). **Corollary (2026-06-28):** enforce the sole-empty rule at the AUTHORING surface too — the Builder's display pre-fill default AND the client serialize default must both keep a lone item's label empty and AGREE, else open+save promotes it to "P1" and re-diverges local-vs-bundle (the render guard alone isn't enough once a header can write the label). | active |
| [0035](0035-fan-out-run-per-procedure-aggregate-only-when-multi-distinct-items-examined.md) | 2026-06-28 | backend | When a control's run fans out into per-procedure `RunRecord`s, persist a separate control-level `procedure_id=''` aggregate **only when `len(per_proc_runs) > 1`** — a single-procedure control persists exactly ONE run; never gate the aggregate on "frames available"/"always" (it double-persists every single-procedure control, inflating run history + the bundle — demo 9→16 runs). Report population as **distinct items examined** = union of distinct item-keys across each test's **INPUT** frame (`node_frames[test.inputs[0]]`, NOT its violating output), degrading to the run population when frames are absent ([[0013]]); the aggregate **dedupes violations by item-key** (one exception per item via `details["checks"]`, JSON-native [[0020]]). Prove the run-count **and** population invariant with a test per control SHAPE (single vs multi procedure) — one-shape unit tests stay green while the invariant breaks ([[0014]]). | active |
| [0034](0034-triage-works-for-me-bugs-with-a-fresh-install-then-treat-corrupted-state-as-first-class.md) | 2026-06-28 | process | Triage a "broken on my machine, can't reproduce in the dev tree" report for a **stateful local tool** by reproducing a **genuine fresh install FIRST** — clean venv, the *built wheel* (not an editable install), the *real server* (not a dev-tree `TestClient`), over a fresh project dir, asserting an identity string so you know which code ran ([[0019]], [[0003]]). If fresh install passes, the failure is in **persisted/user state**: treat that as a first-class failure mode — make the path degrade gracefully, never crash ([[0033]]), AND ship a one-click reset/recovery (`reset_to_demo`), don't patch the single corrupt row — and drive the fix from empirically reproduced corrupt states, one regression test each. | active |
| [0033](0033-never-500-is-convert-at-source-plus-backstop-not-an-exception-allowlist.md) | 2026-06-28 | backend | A control-run's "never a 500" guarantee is enforced by **converting author/data errors to `RunnerError` at the runner** (wrap BOTH the source-load loop AND the `rule_spec` eval path in `run_control`, mirroring the author-callable wrap — so the web app and `uticen-lite run` share one type) **plus a logged broad `except Exception` backstop at the run route** (and a "never raises" wrap over `run_view`), **never an exception-type allowlist**: the route caught only `(RunnerError, ProjectError, KeyError, IndexError)`, but running user logic over user data raises `ValueError` (deleted cross-source `other_source`), pandas `TypeError` (an ordering op `gt`/`lt` on a text column — the SILENT `eq` case is [[0011]], ordering ops RAISE), `ArrowInvalid` (bad regex), and `FileNotFoundError` (missing backing file) — and since the bad state persists in `controlplane.db`, every later Run click 500s ("all the Run buttons are broken"). Prove it with a test per realistic corrupted state asserting non-500, and ship a one-click factory reset (`reset_to_demo`) so a wedged store recovers. Extends [[0013]]. | active |
| [0032](0032-qualify-component-input-rules-to-beat-the-base-attribute-selector.md) | 2026-06-28 | frontend | Qualify a component-scoped `<input>`/`<textarea>` style rule with its `[type=...]` attribute (or a second class) so it out-specifies the global `input[type="text"]`/`textarea`/`select` block in `app.css` — a bare `.component input` is specificity (0,1,1), ties that base rule, and **loses on source order** because the base block is declared later, silently reverting your font-size/padding (the control-title inline editor rendered 13px instead of its intended 24px). Raise the *component* selector's specificity; never fix it by reordering or weakening the shared base (that re-exposes every field — [[0005]]), and verify styled-field intent in a real browser since the cascade tie is invisible in source. **Corollary (2026-06-28):** pin the styled-field intent with an e2e computed-style teeth-check (`to_have_css("font-size", …)`) — the cascade tie is invisible in source AND diff, so only the rendered browser value proves the override held ([[0040]] run-the-real-thing). | active |
| [0031](0031-changing-the-northwind-demo-cardinality-fans-out-to-many-count-assertions.md) | 2026-06-27 | testing | Changing the Northwind demo's control/source count breaks ~7 hardcoded count assertions across 5 test files (`test_build_cmd`/`test_import_cmd`/`test_setup`/`test_wheel_build`/`test_import_service`, not just `test_northwind`) plus the README + PRODUCT-MAP prose — `grep -rn` the whole `tests/` tree for the old literals (`== N`, `(N, N)`, `"N controls"`, `"N runs"`) and update every one; keep a public-API-sourced demo source a frozen CSV so CI stays offline + deterministic ([[0025]]; same fan-out shape as [[0004]]/[[0014]]). **Corollary:** to add a capability showcase to the demo, prefer **CONVERTING** an existing control over ADDING one — converting keeps every `== N` count (tests + README/PRODUCT-MAP prose + packaged wheel counts) stable, confining the fan-out to that control's own run/population/shape assertions (2026-06-28: converting `manual-je-review` to two procedures left every `== 9` untouched, only moved the bundle run count 9→11 [[0035]]); re-attribute exceptions rather than adding them so the seeded total holds. | active |
| [0030](0030-incremental-dag-recompute-via-content-addressed-ancestor-closure-keys.md) | 2026-06-23 | pipeline | Recompute a DAG "from the edited step onward" by content-addressing each node's cached result on `hash(ancestor-closure canonical + {source_id: version_token})`: editing a node busts its key and every descendant's (their closures contain it) while upstream keys hold, and a changed source busts everything downstream — invalidation falls out with no dirty-flagging. Canonicalize topo-ordered with `sort_keys=True`, exclude non-data fields (narrative) so cosmetic edits don't bust, keep the cached compute a pure function of the key (don't read out-of-key state like `pop.key_columns`) so a process-wide cache is safe across controls, bound it LRU, and prove with a recompute spy ([[0009]]). | active |
| [0029](0029-trust-boundary-teeth-check-uses-an-include-false-sentinel-column.md) | 2026-06-23 | testing | A bundle trust-boundary teeth-check seeds its sentinel in an **`include:false`** column (present in `Population.df`, excluded from the workpaper `DataSample`) and asserts zero occurrences across every zip entry + a `manifest.json` sanity assert — because the bundle **zip** legitimately ships a bounded, include-filtered **workpaper sample** as evidence, so seeding an included column false-positives on the workpaper HTML (and token-matching `"rows"` false-positives on `row_count`). The schema-validated **manifest** carries no raw population; the workpaper sample in the zip is a different layer ([[0001]]). | active |
| [0028](0028-key-order-refactors-are-bundle-safe-only-under-sort-keys-serialization.md) | 2026-06-22 | contract | A producer-side serialization refactor that **reorders dict keys** in the bundle manifest is byte-safe **iff** the serializer normalizes order — confirm `bundle/archive.py` still writes `json.dumps(..., sort_keys=True)` before relying on it, and let the contract gate (`tests/test_contract_export.py` + `tests/schema`) prove byte-identity; never depend on insertion order for anything bundle-facing, and dropping `sort_keys=True` re-exposes every producer's key order (`sort_keys` normalizes order, not content) ([[0001]], [[0027]]). | active |
| [0027](0027-adversarially-verify-ai-filed-review-issues-against-the-real-repo.md) | 2026-06-22 | process | Never implement an AI-filed code-review issue from its prose — these tools **hallucinate paths, symbol names, line numbers, and counts** (4 of 10 in batch #55–#64 cited nonexistent files/functions; 2 of the real 6 needed corrected scope). Fan out **one adversarial verifier per issue** that re-derives every concrete claim from the actual repo (grep whole tree incl. `tests/`/`examples/` for "unused"; read all cited sites for "duplicated"; confirm the file exists), returns a structured VALID/PARTIAL/INVALID → IMPLEMENT/MODIFIED/REJECT verdict with **corrected** scope, and judges value-vs-churn against the minimal-surface/brittle-by-design values — reject mass-renames whose cited symbols don't exist ([[0001]]). | active |
| [0026](0026-isolate-plaintext-secrets-warn-loudly-and-guard-the-bundle-boundary-with-a-positive-test.md) | 2026-06-22 | security | When persisting credentials in the local store, isolate them in a dedicated store-only table, render a **loud plaintext-at-rest warning on every surface** that enters or displays a credentialed source (not just the form), and add a **positive regression test** that builds a bundle from a credentialed source and asserts the token/URL/sheet are absent from every bundle entry (the contract gate's structural silence isn't enough; include a teeth-check that the guard fails on a real leak). Store-only — never touches the bundle `schema_version` ([[0001]], [[0017]]). | active |
| [0025](0025-one-time-snapshot-to-file-honors-the-no-live-connectors-non-goal.md) | 2026-06-22 | product | Satisfy a "bring data from a REST/remote source" request without breaking the "no live connectors / file-first" non-goal by making the fetch a **one-time, user-initiated snapshot-to-file**: the local snapshot (not the endpoint) is the source of truth, there is no scheduler/poll/background read, and re-fetch is an explicit action routed through the existing review→confirm diff (never a silent overwrite). Surface the non-goal conflict and get the reinterpretation approved before building ([[0017]]). | active |
| [0024](0024-file-ingest-funnel-catches-both-missing-engine-and-corrupt-content-at-every-route.md) | 2026-06-22 | backend | A user-file ingest funnel must convert **both** "optional engine absent" (`ImportError`→`AdaptersUnavailable`) **and** "content corrupt/unreadable" (`BadZipFile`/`ArrowInvalid`/`UnicodeDecodeError`→a typed `TableParseError`) into friendly errors — catch `ImportError` **before** the broad `except Exception` so a missing engine isn't mislabeled corrupt, scope the broad catch to the read call and re-raise typed (never swallow [[0008]]), and catch **both** at every entry point (create, preview, URL-create, re-fetch, **and** refresh), not just create. | active |
| [0023](0023-thread-a-new-upsert-column-through-every-writer-or-it-nulls-on-update.md) | 2026-06-22 | store | A new column on an `ON CONFLICT DO UPDATE SET col=excluded.col` upsert silently writes **NULL** on every caller that omits the kwarg (the UPDATE branch overwrites, not preserves) — audit every call site of that upsert and thread the value through each (especially refresh/confirm/save, not just create), and add a regression test that the value **survives an UPDATE** (a create-only test passes while every update writer nulls it). Write-path analogue of [[0014]]; pairs with [[0006]]. | active |
| [0022](0022-bump-the-store-schema-version-constant-when-adding-a-migration-step.md) | 2026-06-22 | store | When you append a step to `_STEPS` in `store/migrations.py`, **bump the STORE `SCHEMA_VERSION` constant in the same commit** (and any test hardcoding the old integer) — existing tests assert `user_version == SCHEMA_VERSION`, and a per-step test asserting `user_version >= N` won't catch the break; run the whole `tests/store` suite before the task gate. Do NOT touch the **bundle** `schema_version` in `schema/__init__.py` ([[0001]]). | active |
| [0021](0021-detect-join-misses-via-a-brought-marker-not-the-coalesced-key.md) | 2026-06-22 | pipeline | A Custom Python node behind a Join reaches the other source only via `bring_columns` (never `sources[...]`); to detect an unmatched LEFT-join row, test a **brought non-key column that's always present on a match** (e.g. `invoice_date`) — NOT the join key (the merge coalesces the same-named key into one always-present column) and NOT a `<key>_joined` column (never emitted; `suffixes=('', '_joined')` only suffixes colliding **non-key** columns like `amount_joined`); prove equivalence by diffing violation item_keys vs the pre-change run ([[0009]]), and keep the node allowlist-clean ([[0008]]: no `import pandas`, operate on the `rows` frame). | active |
| [0020](0020-keep-violation-details-json-native-sanitize-at-from-raw.md) | 2026-06-22 | data-integrity | Keep violation `details` JSON-native by sanitizing once at `Violation.from_raw` (the single funnel for the rule, pipeline, and Python paths) — a no-code condition on a `date`/`number`/`boolean` column emits a generic `{col: row[col]}` that leaks a pandas `Timestamp`/numpy scalar/`NaT` into `details` and breaks `json.dumps` on run-persist; duck-type the coercion (Timestamp→isoformat, NaT/NaN→None **before** isoformat, numpy→`.item()`) so `model/` stays pandas-free, and note `details` never enter the bundle so `schema_version` is untouched. | active |
| [0019](0019-put-the-worktree-on-pythonpath-when-verifying-its-code-with-an-adhoc-script.md) | 2026-06-22 | process | When verifying a worktree's changes by **running code** (a render/screenshot/repro script, not `pytest`/`python -m` from the worktree), put the worktree root on the path ahead of site-packages — `PYTHONPATH="$PWD" python /abs/script.py` or run from inside the worktree — because `python /abs/script.py` prepends the *script's* dir (not cwd) to `sys.path`, so `import <pkg>` resolves to the **editable-installed main checkout**, silently rendering the OLD code; assert a string the change introduced is present before trusting the output. | active |
| [0018](0018-capture-learnings-before-opening-an-auto-merging-pr.md) | 2026-06-22 | process | In a repo that auto-merges maintainer PRs on green, author and commit the cycle's learnings to the feature branch **before** opening the PR (auto-merge squash-lands the instant CI passes, freezing the PR before a post-finishing learnings commit exists); if learnings are authored after the merge, cut a fresh branch from updated `main` and land them in a clean follow-up PR — verify "fully merged" by an empty `git diff main..branch` tree diff, not `git cherry` (squash defeats patch-id matching). | active |
| [0017](0017-opt-in-egress-must-default-off-reword-the-claim-and-prove-off-makes-no-call.md) | 2026-06-22 | security | Any new network-reaching capability defaults OFF, rewords **every** copy of the "zero egress" claim to "by default … except `<named opt-in>`", gates proactive paths on the toggle (return before any network call when OFF), and proves OFF with a **call-counter** test that asserts the network function ran **zero times** (an empty-output assertion can pass while still making the call) — only explicit user-initiated checks may egress. **Corollary (2026-06-29):** flipping the egress **default OFF→ON** is the SAME fan-out — it re-falsified "zero egress by default" in README/INSTALL/PRODUCT-MAP (PR #110 missed them); re-grep + reword EVERY copy (retire "zero egress by default" → lead with "client data never leaves; on by default, disable for zero egress"), and flip the OFF-path tests to set the toggle OFF explicitly + add `test_default_is_true`. It's a product-direction change → `setting-strategy`. | active |
| [0016](0016-self-replacing-process-upgrades-via-a-detached-helper.md) | 2026-06-22 | backend | A long-running process upgrading the package it is itself running must never reinstall in-process: spawn a **detached, self-contained, stdlib-only helper** (importing nothing from the package under replacement), have it **wait for the parent PID to fully exit before mutating files**, surface the result via a **one-shot status file** read on next launch, flush the response and shut down (no hot-restart, no auto-upgrade), and make spawn/shutdown injectable so tests never spawn or kill; a short-lived CLI running the same upgrade can run inline. | active |
| [0015](0015-verdicts-and-thresholds-are-render-store-only-never-in-the-bundle.md) | 2026-06-22 | contract | A control's threshold and pass/fail determination are render + store concerns — never serialize them into the bundle; to add per-X verdict semantics, store the inputs (counts, thresholds) in store-only state and COMPUTE the verdict at render, keeping `schema_version` frozen and the bundle carrying only raw results + the (already unbounded) `workpaper.procedures` array. Group existing store rows by the new key (e.g. `runs.procedure_id`) — don't invent a new bundle shape. | active |
| [0014](0014-audit-every-singular-accessor-call-site-when-a-model-gains-a-plural.md) | 2026-06-22 | refactor | When a model gains a PLURAL accessor where a singular existed and you keep the singular as a back-compat alias (`singular = plural[0]`), that alias silently hides every consumer that should now handle ALL elements — grep every singular call site and decide per-site one-vs-all, add a multi-element fixture per consumer, and do a whole-branch sweep (per-task TDD + single-element fixtures can't catch the miss). | active |
| [0013](0013-derived-editor-state-must-round-trip-and-tolerate-incomplete-graphs.md) | 2026-06-21 | frontend | When an editor renders a DERIVED / in-progress representation, the round-trip and every side-effect must follow the rendered thing, not the stored thing: submit the derived graph (not the stored one), re-derive ALL side-effects of any relocated save path (e.g. cross-source source-binding) from the actual graph, re-home every affordance the old surface carried, and make live preview probes degrade (never 500) on incomplete graphs (2026-06-27: extended to the **run** + step-**inspector** boundaries — a half-authored control renders a friendly "not ready" page, and a "never raises" helper wraps its whole body incl. pre-`try` loads); (2026-06-28: also **don't let an "add step" affordance CREATE invalid state** — default a new terminal's upstream to a valid node so an unwired Test can't 422/vanish). | active |
| [0012](0012-rerun-e2e-browser-smoke-on-htmx-swap-changes.md) | 2026-06-20 | testing | Re-run + update the e2e browser smoke (`pytest tests/e2e -m browser`) whenever you change an HTMX swap that restructures a `plane/` form in place **or change a run-aggregation/render semantic** (e.g. dedupe-by-item-key, a heading format) — isolated partial-render unit tests and diff review can't see the assembled post-swap DOM (duplicate/upgraded elements), so never dismiss an e2e strict-mode/locator failure as flaky without reading it; when it IS a stale expectation, confirm the new value is correct before updating it ([[0035]]). **Corollary (2026-06-28):** a removed/renamed `data-*` DOM hook fans out to `-m browser` e2e suites that the default `pytest -q` + a per-task gate (e.g. `tests/plane`) SKIP — grep the WHOLE tests/ tree for the attribute and run the browser gate; migrate the dropped assertion to its new home ([[0031]]/[[0014]]/[[0038]] fan-out family). | active |
| [0011](0011-match-condition-value-type-to-the-columns-loaded-dtype.md) | 2026-06-20 | data-integrity | Match a rule condition's value type to the bound column's LOADED dtype — a bool/number literal silently matches nothing against a text-loaded column; apply the source type mapping first (or use a dtype-agnostic op like `not_empty`/`regex`) and run #9's coercion check. | active |
| [0010](0010-new-authoring-representation-compiles-to-the-existing-artifact.md) | 2026-06-20 | store | Add a new authoring representation (e.g. the pipeline graph) as store-only state plus a COMPILE step to the existing bundle artifact (rule_spec/test_code); bump the store schema, never `schema_version`, never add the new shape to `bundle.schema.json` — keep the simple case compiling to a `rule_spec`. | active |
| [0009](0009-prove-generated-code-equals-the-interpreter.md) | 2026-06-20 | testing | When a code path GENERATES executable code from a spec/graph, ship an equivalence test that `exec`-and-compares the generated output against the canonical interpreter (`evaluate_rule`) on real fixtures incl. `any`/`all` + cross-source — a generated-vs-interpreter divergence is a release blocker. | active |
| [0008](0008-run-untrusted-authored-code-behind-three-layers.md) | 2026-06-20 | security | Run untrusted authored code behind three layers: an allowlist-AST lint at save that ALSO blocks indirection (`__builtins__[...]`, `getattr`, dunder attrs, `importlib`/`__import__`), lexical starvation at compile (a module-level fn the capability can't reach), and a hard re-scan that REFUSES output at the export boundary. | active |
| [0007](0007-control-plane-editors-are-server-rendered-sub-route-tabs.md) | 2026-06-20 | frontend | Model a multi-section control-plane editor as server-rendered `GET` sub-route tabs (`/x`, `/x/data`, `/x/history`) sharing one `_*_tabs.html` nav include + an `active` key — not client-side JS tabs; register the specific sub-routes so the `/{id}` route can't shadow them. | active |
| [0006](0006-evolve-source-state-without-touching-the-bundle.md) | 2026-06-20 | store | To add richer per-source/per-file authoring state, put it in a store-only table and keep the single bundle-facing column (`sources.extract_date`) as a denormalized MIRROR of the authoritative value, re-synced on every write path — never thread new fields into `to_data_source()`/`bundle.schema.json`. Corollary: SQL backfills strip a known prefix with `substr(path, length('data/')+1)`, not replace-all. | active |
| [0005](0005-control-plane-reuses-workpaper-design-tokens.md) | 2026-06-19 | frontend | The control-plane UI (`plane/static/app.css`) and the workpaper renderer (`render/html.py`) share one design language but NOT a stylesheet — keep the duplicated palettes in sync, and route every color through a `var(--token)` so theming (the `[data-theme=light]` override + `--cm-*` CodeMirror tokens) stays a one-place change; apply the saved theme before first paint. Corollary: restyle ONE page's use of a shared structural class (`.page-head`, in ~20 templates) via a **modifier class** (`.control-head`), never by mutating the base rule. | active |
| [0004](0004-ordering-seam-audit-positional-consumers.md) | 2026-06-19 | data-integrity | When a query's sort order changes (`ORDER BY ... DESC` for a newest-first UI), audit every positional consumer (`x[-1]`/`x[0]`) — a reorder silently breaks "pick the latest/first" code; single-record fixtures can't catch it, so use a 2+ record fixture + a whole-branch review. | active |
| [0003](0003-hatch-wheel-no-force-include-package-data.md) | 2026-06-19 | packaging | Hatchling `packages = ["pkg"]` already ships package-internal data files; `force-include`-ing a path already inside the package fails the wheel build (duplicate archive path). Force-include only out-of-package/VCS-ignored files; verify packaging by BUILDING the wheel, not by parsing pyproject. **Corollary:** to ship out-of-package data (a demo/example) to pip users, force-include it into the package — but force-include skips editable installs, so runtime readers MUST resolve packaged-first with a repo-path fallback, and prove it from a clean-venv wheel install outside the repo. | active |
| [0002](0002-fastapi-sqlite-per-handler-connection.md) | 2026-06-19 | backend | **`connect()` must open sqlite with `check_same_thread=False`** — FastAPI's threadpool runs a sync `Depends` generator's setup and the sync endpoint on DIFFERENT threads under concurrency (they share one only sequentially, which is why dev + single-threaded `TestClient` masked it), so a per-request conn created in setup and used in the handler threw cross-thread `ProgrammingError` and 500'd every `Depends(get_conn)` GET (most visibly the header update-indicator, fired concurrently with each page load). Also open a per-handler connection (try/finally) in async/writing handlers. (2026-06-29 correction: the original "sync GETs share the threadpool thread" claim was false.) Also: Starlette ≥1.3 `TemplateResponse(request, name, ctx)`. | active |
| [0001](0001-stay-compatible-with-the-uticen-app.md) | 2026-06-19 | contract | ⚑ Cardinal: `contract/bundle.schema.json` is the binding contract with the Uticen app — every manifest-touching change must keep the bundle schema-valid (gate: `tests/test_contract_export.py` + `tests/schema/test_bundle_schema.py`); all producers reuse `assemble_bundle`/`write_bundle`; never put raw population in the bundle; evolve the schema on both sides together. | active |
