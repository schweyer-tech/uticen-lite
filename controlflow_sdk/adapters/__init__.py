"""Adapters package — CPython-only data-loading layer.

Import the public API from sub-modules:
  from controlflow_sdk.adapters.base import Source
  from controlflow_sdk.adapters.files import CsvSource, source_for, UnsupportedSourceError
"""
