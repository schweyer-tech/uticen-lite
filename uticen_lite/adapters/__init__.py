"""Adapters package — CPython-only data-loading layer.

Import the public API from sub-modules:
  from uticen_lite.adapters.base import Source
  from uticen_lite.adapters.files import CsvSource, source_for, UnsupportedSourceError
"""
